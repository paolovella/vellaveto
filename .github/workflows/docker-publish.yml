name: Docker Build & Publish

on:
  # Tag trigger removed — release.yml orchestrator handles release Docker builds.
  # Branch push kept for continuous main builds.
  push:
    branches: [main]
    paths:
      - "Dockerfile"
      - "Cargo.toml"
      - "Cargo.lock"
      - "examples/**"
      - "vellaveto-types/**"
      - "vellaveto-engine/**"
      - "vellaveto-audit/**"
      - "vellaveto-config/**"
      - "vellaveto-canonical/**"
      - "vellaveto-mcp/**"
      - "vellaveto-discovery/**"
      - "vellaveto-cluster/**"
      - "vellaveto-approval/**"
      - "vellaveto-server/**"
      - "vellaveto-proxy/**"
      - "vellaveto-http-proxy/**"
      - "vellaveto-tls/**"
      - ".github/workflows/docker-publish.yml"
      - "scripts/check-docker-defaults.sh"
  pull_request:
    branches: [main]
    paths:
      - "Dockerfile"
      - "Cargo.toml"
      - "Cargo.lock"
      - "examples/**"
      - "vellaveto-types/**"
      - "vellaveto-engine/**"
      - "vellaveto-audit/**"
      - "vellaveto-config/**"
      - "vellaveto-canonical/**"
      - "vellaveto-mcp/**"
      - "vellaveto-discovery/**"
      - "vellaveto-cluster/**"
      - "vellaveto-approval/**"
      - "vellaveto-server/**"
      - "vellaveto-proxy/**"
      - "vellaveto-http-proxy/**"
      - "vellaveto-tls/**"
      - ".github/workflows/docker-publish.yml"
      - "scripts/check-docker-defaults.sh"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build & Push Container Image
    runs-on: ubuntu-latest
    timeout-minutes: 240
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Set up QEMU (multi-platform)
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Check Docker secure defaults
        run: bash scripts/check-docker-defaults.sh

      - name: Check Dockerfile syntax
        run: docker build --check .

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          platforms: ${{ github.event_name == 'pull_request' && 'linux/amd64' || 'linux/amd64,linux/arm64' }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan image for vulnerabilities
        if: github.event_name != 'pull_request'
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH
          exit-code: '1'

      - name: Upload Trivy scan results
        if: ${{ always() && github.event_name != 'pull_request' && hashFiles('trivy-results.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v3
        with:
          sarif_file: trivy-results.sarif
